<Window x:Class="ExamTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExamTimer"
        mc:Ignorable="d"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        WindowStartupLocation="Manual"
        Title="MainWindow" Height="320" Width="600">
    <Border Background="#4000" Padding="10" HorizontalAlignment="Right" VerticalAlignment="Top">
        <Grid>
            <Grid.Resources>
                <Style TargetType="Label">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="HorizontalAlignment" Value="Right"/>
                </Style>
            </Grid.Resources>
            
            <StackPanel Orientation="Vertical">
                <Label FontSize="40" x:Name="Label_Text"/>
                <Label Content="NaN 天" FontSize="70" x:Name="Label_D"/>
                <Label Content="N 时 N 分 N 秒" FontSize="60" x:Name="Label_HMS"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
